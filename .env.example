# Xuantong Config 环境变量模板
# 复制为 .env 后填写随机高强度值；.env 已被 Git 忽略。

# Docker Compose 数据库配置
DB_PASSWORD=replace-with-a-strong-database-password

# Broker 与配置加密密钥，生产环境必须设置，建议使用独立的 32+ 字节随机值。
BROKER_SECRET_KEY=replace-with-a-random-broker-secret
CONFIG_ENCRYPT_KEY=replace-with-a-random-encryption-key
